public enum GameState {
    MENU, PLAYING, PAUSED, GAME_OVER, WIN, HELP, SETTINGS, TRANSITION,
    ENDLESS,        // Endless mode active gameplay
    UPGRADE_SELECT  // Power-up selection overlay (Endless mode)
}